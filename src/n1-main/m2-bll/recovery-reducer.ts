const initialState = {
    login:true
}
export const RecoveryReducer = (initialState:any= {}, action:any)=>{
    switch (action.type){
        case 'SET-LOGIN':
            return {...{}}
        default:
            return {...{}}
    }
}