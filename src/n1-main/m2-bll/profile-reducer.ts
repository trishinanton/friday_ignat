const initialState = {
    login:true
}
export const ProfileReducer = (initialState:any= {}, action:any)=>{
    switch (action.type){
        case 'SET-LOGIN':
            return {...{}}
        default:
            return {...{}}
    }
}