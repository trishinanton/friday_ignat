const initialState = {
    login:true
}
export const NewProfileReducer = (initialState:any= {}, action:any)=>{
    switch (action.type){
        case 'SET-LOGIN':
            return {...{}}
        default:
            return {...{}}
    }
}