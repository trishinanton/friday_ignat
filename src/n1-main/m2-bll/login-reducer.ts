
const initialState = {
    login:true
}
export const LoginReducer = (initialState:any= {}, action:any)=>{
    switch (action.type){
        case 'SET-LOGIN':
            return {...{}}
        default:
            return {...{}}
    }
}