const initialState = {
    login:true
}
export const RegisterReducer = (initialState:any= {}, action:any)=>{
    switch (action.type){
        case 'SET-LOGIN':
            return {...{}}
        default:
            return {...{}}
    }
}